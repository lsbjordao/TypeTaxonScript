// Import annotation classes
import { Sources } from "../Sources"

export class Filaments extends Sources {
    /**
     * Apex of lobes.
     */
    colour: 'pinkish' | 'yellowish' | 'whitenish'

    /**
     * Creates an instance of lobes of corolla.
     */
    constructor() {
        super()
    }
}